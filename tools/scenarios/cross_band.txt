# Cross-band switching: walks 2 -> 9 -> 2 -> 13 -> 2 to exercise band-relay pulses.
# Pulse counts assume a fresh looper restart with previous_values.pkl wiped:
#   2  -> band 1, freq 2   (no pulses; hardware default)
#   9  -> band 2, freq 18  (1 band, 2 up; first-entry sets band-2 freq=16)
#   2  -> band 1, freq 2   (4 band — wraps 2->3->4->5->1; freq already tuned)
#   13 -> band 2, freq 22  (1 band; visited so freq stays at 18; +4 up)
#   2  -> band 1, freq 2   (4 band wrap; freq already at 2)
# Totals: band=10, up=6, down=0.
#
# IMPORTANT: restart the looper before running. The runner can wipe the pickle
# but cannot reset the looper's in-memory previous_band / previous_channel.
#
# setup: reset-state
# expect: no-double-spawn
# expect: cleanup-final-player
# expect: relay-pulses-band = 10
# expect: relay-pulses-up = 6
# expect: relay-pulses-down = 0
# delta_seconds  channel
0.0   2
6.0   9
14.0  2
22.0  13
30.0  2
